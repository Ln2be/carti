// Re-export the user's App component so the file you added becomes the app entry point.
// The file at `app/(tabs)/index.tsx` will be imported and used as the root component.
import App from './app/(tabs)/index';

export default App;
